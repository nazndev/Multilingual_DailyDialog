# Preprocess a capped subset including validation:
# - 800 train
# - 200 validation
# - 200 test
# (Total 1200 dialogues.)
# Paths are resolved relative to DATA_DIR.

raw_dir: raw
processed_dir: processed_1000

max_dialogues_per_split:
  train: 800
  validation: 200
  test: 200
