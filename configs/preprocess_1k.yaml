# Preprocess only a small subset for fast iteration.
# Paths are resolved relative to DATA_DIR.

raw_dir: raw
processed_dir: processed_1k

# Limit number of dialogues per split written to processed jsonl.
max_dialogues_per_split: 1000
